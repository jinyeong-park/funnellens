Here are 5 UI/UX improvements I need. Please address them in order:

1. Fix layout: "Step 1" and "Tar: $30" should be on same line. UI looks disorganized.

2. Confirm logic: Step 1 = CPM, Step 2 = CPC, Step 3 = CPL, Step 4 = CPA. Correct?

3. Visibility issue: Each step is not clearly labeled. As a UX designer, what's the best UI pattern? Please implement vertical stepper or progress bar with color-coded status (red = fail, yellow = blocked, green = pass).

4. Demo mode: I want to see alert messages for different scenarios (high CPM, high CPC, etc.) without changing real data. Add toggle buttons or a demo mode.

5. Partial data: Don't force all 4 CSV files. Make them optional. Run diagnosis on whatever data exists. Skip missing steps gracefully.

Please implement all 5 improvements.
